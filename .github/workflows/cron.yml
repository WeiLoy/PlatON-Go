name: cron_deploy

on:
  schedule:
    branches: test_actions
    - cron: "*/6 * * * *"

jobs:
  stale:

    runs-on: [self-hosted, Linux, X64, skip]
    steps:
    - name: touch_file
      run: sh /home/juzix/test.sh
